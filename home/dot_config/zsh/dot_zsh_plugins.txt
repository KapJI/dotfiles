# Antidote plugins
#
# Load order matters here. Constraints:
#  - fzf-tab must load AFTER compinit (it wires into compsys at load
#    time). zephyr's completion plugin runs compinit synchronously
#    when its `immediate` zstyle is set (see config.d/antidote.zsh).
#  - fast-syntax-highlighting must load AFTER compinit + fzf-tab.
#    Otherwise its widget wrappers (complete-word, expand-or-complete,
#    fzf-tab-complete) end up applied to placeholder widgets that get
#    redefined later, leaving completed text un-highlighted until a
#    subsequent keystroke triggers a redraw.
#  - zsh-autosuggestions must be LAST: it wraps the final ZLE widget
#    set, including fsh's already-wrapped versions.

romkatv/powerlevel10k

zsh-users/zsh-completions path:src kind:fpath
MichaelAquilina/zsh-you-should-use

# compinit runs synchronously here (zephyr `immediate` zstyle).
mattmc3/zephyr path:plugins/completion

# fzf-tab requires compinit to have run.
Aloxaf/fzf-tab

# fsh wraps widgets at source time; must come AFTER fzf-tab so
# fzf-tab-complete and friends are already created.
zdharma-continuum/fast-syntax-highlighting

# OMZ
getantidote/use-omz
ohmyzsh/ohmyzsh path:lib
ohmyzsh/ohmyzsh path:plugins/colored-man-pages
ohmyzsh/ohmyzsh path:plugins/dirhistory
ohmyzsh/ohmyzsh path:plugins/extract
ohmyzsh/ohmyzsh path:plugins/fancy-ctrl-z
ohmyzsh/ohmyzsh path:plugins/safe-paste
# shrink-path provides shrink_path() called by config.d/auto_title.zsh.
ohmyzsh/ohmyzsh path:plugins/shrink-path

# Must be last — wraps the final widget set from everything above.
zsh-users/zsh-autosuggestions
