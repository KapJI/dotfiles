{{ if and (eq .chezmoi.os "linux") .is_desktop -}}
#!/usr/bin/env bash
set -euo pipefail

# Run daily. Current day: {{ now.YearDay }}, {{ now.Year }}
#
# Anthropic's installer is idempotent and always pulls the latest release;
# re-running it daily upgrades claude in place.
curl -fsSL https://claude.ai/install.sh | bash
{{ end -}}
